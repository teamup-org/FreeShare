import React from 'react';
import Header from './Header';

const UpdateProfile = () => {
    return (
        <div>
            <Header />
            <h1>Update Profile</h1>
        </div>
    );
};

export default UpdateProfile;