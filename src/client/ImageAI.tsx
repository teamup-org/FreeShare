import React from 'react';



const ImageAI = (image: any, description: string) => {
    return (
        <div>
            <h1>Image AI</h1>
            <p>{description}</p>
        </div>
    );
};

export default ImageAI;